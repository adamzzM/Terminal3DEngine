[package]
name = "engine3D"
version = "0.1.0"
edition = "2024"



[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["wincon", "processenv","winbase","consoleapi"] }
